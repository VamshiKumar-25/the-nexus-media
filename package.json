{
  "name": "nexus-media-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:server": "npm install --prefix server",
    "install:client": "npm install --prefix client",
    "build:client": "npm run build --prefix client",
    "setup": "npm run install:server && npm run install:client && npm run build:client"
  }

}